services:
  backend-server:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - backend-network

networks:
  backend-network:
